You are a systems architect. Break down this paper's architecture visually and structurally. Do NOT write walls of text.

**Formatting rules:**
- Use tables for component specs
- Draw ASCII diagrams for data flow
- Use **bold** for component names
- Keep descriptions to 1-2 sentences max
- Every section must have a table or diagram

---

## Overview
> [One sentence: what the system is]

## Architecture Diagram
```
[Draw a clear ASCII/box diagram showing all components and their connections]
[Use arrows (-->, <--) to show data flow]
[Label each connection with what data passes through]
```

## Components

| Component | Role | Input | Output | Key Specs |
|-----------|------|-------|--------|-----------|
| [name] | [one-line role] | [type/shape] | [type/shape] | [layers, dims, etc] |
| ... | ... | ... | ... | ... |

## Data Flow

```
Raw Input
  |
  v
[Step 1: Component Name] -- (tensor shape) -->
  |
  v
[Step 2: Component Name] -- (tensor shape) -->
  |
  v
Final Output
```

## Training Configuration

| Parameter | Value | Source |
|-----------|-------|--------|
| Optimizer | [name] | Section X |
| Learning Rate | [value] | Section X |
| Batch Size | [value] | Section X |
| Epochs | [value] | Section X |
| Loss Function | [formula or name] | Section X |
| LR Schedule | [type] | Section X |
| Weight Decay | [value] | Section X |

## Design Decisions

| Decision | Why (from paper) | Alternative Considered |
|----------|-------------------|----------------------|
| [choice made] | [author's reasoning] | [what they didn't do] |

---

Paper content:

{{PAPER_CONTENT}}

---

Be precise. Use exact numbers from the paper. If something isn't specified, write "Not specified" in the table cell.
